{
  "name": "frenchforet-course",
  "version": "0.1.0",
  "description": "FrenchForet LLM course — Docsy/Hugo site build dependencies",
  "private": true,
  "scripts": {
    "dev": "hugo server",
    "build": "hugo --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.25",
    "postcss-cli": "^11.0.0"
  }
}
